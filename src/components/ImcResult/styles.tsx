import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  resultContainer: {
    flex: 1,
    marginTop: 15,
    paddingTop: 60,
    borderRadius: 50,
    alignItems: 'center',
    width: '100%',
  },
  infoText: {
    fontSize: 18,
    color: '#FF0043',
    fontWeight: 'bold',
  },
  resultText: {
    fontSize: 48,
  }
});

export default styles;
